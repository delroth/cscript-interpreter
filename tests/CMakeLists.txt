include(BoostTestTargets)
add_boost_test(cscript
    SOURCES
        runner.cc
        script-loader.cc
        script-loader.hh
        test-exit.cc
        test-reserve.cc
    TESTS
        instr_exit
        instr_reserve
    LIBRARIES
        cscript
        ${Boost_LIBRARIES}
    )
